import { ServiceCategory } from '../types/services';

export const serviceCategories: Record<ServiceCategory, string> = {
  individual: 'Individual Services',
  family: 'Family Services',
  child: 'Child & Youth Services',
  specialty: 'Specialty Services',
};